version: "3.8"

services:
  postgres-test:
    image: postgres:16-alpine
    container_name: adpilot-test-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: adpilot_test
      POSTGRES_PASSWORD: adpilot_test_pw
      POSTGRES_DB: adpilot_test
    volumes:
      - postgres_test_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U adpilot_test -d adpilot_test"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis-test:
    image: redis:7-alpine
    container_name: adpilot-test-redis
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_test_data:
