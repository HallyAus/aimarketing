# Continue Here — AdPilot Session Handoff

**Last Updated:** 2026-04-03 ~2:30 AM AEST
**Last Commit:** `40cf847` — full feature descriptions on every page
**Branch:** master
**Deploy Status:** Vercel daily deploy limit hit (100/day). Latest code pushed to GitHub but not yet deployed. Will auto-deploy on next push after limit resets (~24h from last deploy burst).

---

## What Was Built This Session

### Two Complete Projects

**1. AdPilot SaaS Platform** (`D:\Claude\Projects\aimarketing`)
- Turborepo monorepo: Next.js 15 + BullMQ worker + Prisma + 5 packages
- Deployed on Vercel + Neon PostgreSQL
- URL: https://aimarketing-danieljhall-mecoms-projects.vercel.app
- GitHub: HallyAus/aimarketing
- 105+ pages, 50+ API routes, 27 DB models, 44 indexes

**2. AdPilot Agency Site** (`D:\Claude\Projects\adpilot-site`)
- Next.js 15 standalone marketing site
- Deployed on Vercel: https://adpilot-site.vercel.app
- GitHub: HallyAus/adpilot-site
- 6 pages, 18 components, 74 tests

### 30+ Features Implemented
Content: Brand Voice, Competitor Spy, Competitor Match, Hashtags, Image Gen, Video Scripts, Repurposer, A/B Variants, Carousel Builder, Story Templates, Calendar Heatmap, Custom Prompts, Keyword Scanner
Analytics: Dashboard Widgets, Benchmarking, Best Times, Sentiment, ROI, Weekly Email, Audience, Trending
Automation: Auto-Reply, Lead Capture, Approvals, RSS-to-Social, Webhooks, Multi-Language, UTM Builder
Integrations: Facebook Publishing (real), GBP (placeholder), Email Marketing, Landing Pages, CRM (placeholder), White-Label Reports
Infrastructure: Auth (email/pw + Google + Microsoft + magic link), Page-based DB segregation, Vercel Cron, Timezone support, Business Profile settings

### Key Architecture Decisions
- **Page model** is the segregation key — every post belongs to a Page (FK). Account selector stores Page.id (cuid). All queries use `getActivePageId()` + `pageWhere()`.
- **Prisma client** generated to `packages/db/generated/client` and committed to git (solves Vercel pnpm monorepo engine binary issue)
- **`serverExternalPackages: ["@prisma/client", "prisma"]`** in next.config.ts
- **Auth.js v5**: cookie name `__Secure-authjs.session-token`, uses `AUTH_SECRET`, middleware reads with explicit `cookieName`
- **Vercel Cron** (daily) + dashboard widget polling (30s) triggers scheduled post publishing

---

## Current State / What's Working

- ✅ Auth (email/password login working, user: danieljhall@me.com / printforge)
- ✅ Facebook connected (3 pages: Printforge + 2 others)
- ✅ Real Facebook publishing with page-specific tokens
- ✅ All 30+ features have pages and API routes
- ✅ Page-based account segregation across entire dashboard
- ✅ Every sidebar item has tooltip descriptions
- ✅ Every page has descriptive subtitles
- ✅ 117 API tests written
- ✅ 5 critical security issues fixed
- ✅ Mobile responsive + accessible

---

## Known Issues / Bugs

1. **Vercel deploy limit hit** — latest 2 commits (`9e82204`, `40cf847`) are on GitHub but not deployed. Need to wait for limit reset or redeploy manually.
2. **Dashboard "Something went wrong"** — was caused by missing `metadata` column on Organization table in Neon. FIXED by running `ALTER TABLE "Organization" ADD COLUMN IF NOT EXISTS "metadata" JSONB`. Should work now but needs the latest code deployed.
3. **Auto-schedule API may error** — the `isValidTransition` import from `@adpilot/shared` was flagged but should work since it's exported from constants.

---

## Remaining Work (Priority Order)

### Immediate (Next Session)
1. **Redeploy to Vercel** once limit resets — `npx vercel deploy --yes --prod`
2. **Landing page update** — reflect all 30+ features on marketing site
3. **Onboarding tour** — interactive walkthrough after first login
4. **Test the full flow** — generate post → schedule → publish → verify on Facebook

### Security (HIGH)
- Redis-backed rate limiting (Upstash) — in-memory doesn't work across instances
- Zod validation on 9 POST/PATCH routes
- SSRF protection on URL-fetching routes (block private IPs)
- Invitation email sending via Resend
- Stripe payment failure grace period

### Placeholders to Complete
- Google Business Profile — real OAuth
- CRM Integration — real HubSpot/Pipedrive OAuth
- Email Campaign — actually send via Resend
- RSS "Check Feed" — actually parse RSS
- Webhook "Test Rule" — actually test

### Daniel's Morning Checklist
1. Rotate Anthropic API key (was shared in chat!)
2. Google OAuth setup (console.cloud.google.com)
3. Microsoft OAuth setup (portal.azure.com)
4. Verify Resend sending domain
5. Register domain, point to Vercel
6. Facebook live mode (Business Manager)
7. Stripe seed with live keys: `STRIPE_SECRET_KEY=sk_live_xxx pnpm stripe:seed`
8. Re-select Facebook pages in Settings→Connections (creates Page records in new schema)
9. Fill in Business Profile (Settings→Business Profile)
10. Vercel: redeploy once limit resets

---

## Environment Variables on Vercel
DATABASE_URL, AUTH_SECRET, NEXTAUTH_SECRET, NEXTAUTH_URL, NEXT_PUBLIC_APP_URL, MASTER_ENCRYPTION_KEY, ANTHROPIC_API_KEY, FACEBOOK_APP_ID, FACEBOOK_CLIENT_SECRET, FACEBOOK_APP_SECRET, REDIS_URL, CRON_SECRET

## Neon Database
- Project: bitter-recipe-25138172
- Connection: `postgresql://neondb_owner:npg_w5DIAFPmL6MJ@ep-silent-block-akqxsuxu-pooler.c-3.us-west-2.aws.neon.tech/neondb?sslmode=require`
- 27 tables created, all FKs and indexes applied
- `metadata` column added to Organization (just now)

## Resume Command
```
cd D:\Claude\Projects\aimarketing
cat .continue-here.md
```
